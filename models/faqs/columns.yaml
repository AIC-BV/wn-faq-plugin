# ===================================
#  Column Definitions
# ===================================

columns:

    id:
        label: aic.faq::lang.models.general.id
        invisible: true

    category_id:
        label: aic.faq::lang.models.category.label
        relation: category
        select: name
        searchable: true

    question:
        label: aic.faq::lang.models.faq.question
        searchable: true

    answer:
        label: aic.faq::lang.models.faq.answer
        searchable: true
        invisible: true

    is_featured:
        label: aic.faq::lang.models.general.featured_status
        type: featuredstatus

    is_published:
        label: aic.faq::lang.models.general.published_status
        type: publishedstatus

    created_at:
        label: aic.faq::lang.models.general.created_at
        type: datetime
        format: Y/m/d
        invisible: true

    updated_at:
        label: aic.faq::lang.models.general.updated_at
        type: datetime
        format: Y/m/d
        invisible: true
